# 2026-04-27

- 在 `crm-customer-detail-copy` 原型中，将任务日期筛选从两个原生 `<input type="date">` 替换为 Ant Design `DatePicker.RangePicker`，实现点击弹出双月日历选择日期范围；并删除了时间轴底部的“关联订单”按钮。
- 对 `crm-customer-detail-copy-copy` 原型进行视觉/布局/交互优化：修复任务区双重 panel-header 嵌套 bug、统一圆角体系（12px/8px/16px）、统一阴影层级、移除 topbar 粉色边框、优化左侧导航选中态（增加背景+左侧强调条）、增加弹窗/抽屉/遮罩进入动画（keyframes）、优化任务行与时间线 hover 反馈、优化按钮交互态。spec.md 已同步更新。
- `crm-customer-detail-copy-copy`：用户最终决定将时间线高亮恢复为仅高亮最新一条时间线条目（而非整个面板高亮）。回滚了面板整体背景/左侧竖条样式，重新启用 `selectedTimelineEntryId` state 及默认选中 useEffect，仅给最新条目添加 `is-selected` class 和对应 CSS 高亮样式。
